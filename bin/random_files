#! /usr/bin/env ruby

$: << File.expand_path('../../lib', __FILE__)

require 'random_files'

if ARGV.count < 1 or ARGV.first !~ /^\d+/
  puts "USAGE: #{__FILE__} <number-of-files> [dir1, [dir2, [...]]]"
  exit(1)
end

if ARGV.count == 1
  ARGV[1] = '**/*'
end

puts RandomFiles.run(ARGV)
