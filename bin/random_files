#! /usr/bin/env ruby

$: << File.expand_path('../../lib', __FILE__)

require 'random_files'

input     = RandomFiles::Input.new(ARGV)
file_list = RandomFiles::FileList.new(input.files, input.options)

puts file_list.select_files
